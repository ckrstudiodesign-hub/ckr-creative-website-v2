/**
 * Placeholder for Framer's HeaderGallery component (mtEMIYZaW, variant dogCuY3VW)
 * shown in Hero Bottom-Left as a 212px-tall phone-mockup gallery.
 *
 * Currently renders a styled container with a sample asset slot — the actual
 * gallery (phone-mockup carousel with motion) will be ported once we read the
 * mtEMIYZaW component XML and its variants.
 */
export default function HeaderGallery() {
  return (
    <div className="w-full h-[212px] rounded-[20px] overflow-hidden bg-white/[0.06] flex items-center justify-center text-brand-white/40 dm-p14-medium">
      Header Gallery (placeholder — port pending)
    </div>
  )
}
